

# Remove Sign Up from Auth Page

Remove the "Зареєструватися" button and `handleSignUp` function from `src/pages/Auth.tsx`. Only the login form remains — new users can only be created by an admin. No database changes needed; existing accounts are unaffected.

